{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capability set for the installer window. The frontend may invoke the wizard_next command and the core defaults; nothing else (no shell, no fs, no arbitrary http).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-title"
  ]
}
