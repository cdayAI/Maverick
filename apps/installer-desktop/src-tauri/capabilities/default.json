{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capability set for the installer window. The frontend may invoke the `install` command, listen for its progress events, and use core window ops; nothing else (no fs, no arbitrary http from the frontend). The install itself runs the bootstrap process from the Rust side.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-title"
  ]
}
